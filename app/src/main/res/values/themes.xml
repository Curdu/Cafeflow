<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cafeflow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:background"> @color/app_background</item>
    </style>

    <style name="Theme.Cafeflow" parent="Base.Theme.Cafeflow"/>

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/cafeflow_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Cafeflow</item>
    </style>
</resources>